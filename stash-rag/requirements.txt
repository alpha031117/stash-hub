# Phase 1 — indexer + API
fastapi>=0.137
uvicorn>=0.49
psycopg[binary]>=3.3
pgvector>=0.4
pyyaml>=6.0
apscheduler>=3.11
fastembed>=0.8
watchdog>=6.0
pathspec>=1.1

# Phase 2/3 — chat agent + update pipeline (installed now, wired later)
langchain>=1.3
langchain-openai>=1.3
langgraph>=1.2
